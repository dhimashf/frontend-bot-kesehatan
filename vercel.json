{
    "version": 2,
    "builds": [{ "src": "*", "use": "@vercel/static" }],
    "cleanUrls": true,
    "rewrites": [
        {
            "source": "/api/v1/:path*",
            "destination": "$BACKEND_API_URL/api/v1/:path*"
        }
    ]
}